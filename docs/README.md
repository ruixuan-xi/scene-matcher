# 场景库卡片展示前端

静态前端页面，实时从 Supabase 拉取场景数据并以卡片形式展示。

- `index.html` — 页面结构与样式
- `app.js` — 数据拉取、筛选、渲染、导出 Excel 逻辑
- `.nojekyll` — 防止 GitHub Pages Jekyll 处理

部署：在 GitHub 仓库 Settings → Pages → Source 选择 `Deploy from a branch`，分支选 `master`，文件夹选 `/docs`。
